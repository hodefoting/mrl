#!/usr/bin/env luajit

local Mrg    = require('mrg')
local mrg    = Mrg.new(-1, -1);

local path

if (#arg >= 1) then
  path = arg[1]
end

mrg:set_ui(
function (mrg, data)
  mrg:print(path)
  mrg:add_binding("control-q", NULL, NULL, function () mrg:quit() end)
end)

mrg:main()
